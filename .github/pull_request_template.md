## Qué cambia

<!-- En una o dos frases. Si cierra un issue: "Cierra #123". -->

## Cómo se ve

<!-- Capturas de antes y después, y el link al punto (Esc → COMPARTIR → copiar link). -->

## Revisé que

- [ ] `npm run build` pasa (tipos y build).
- [ ] No hardcodeé valores: lo configurable está en `config/*.json`.
- [ ] Los datos, fotos o modelos nuevos son abiertos o míos, con su crédito (nada copiado de Google).
- [ ] Probé el cambio en el navegador.
